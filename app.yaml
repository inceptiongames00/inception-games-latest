runtime: nodejs20

env: standard

instance_class: F1

automatic_scaling:
  min_instances: 0
  max_instances: 2

env_variables:
  GCP_PROJECT_ID: "inception-games"
  GCP_BUCKET_NAME: "inception-games-storage"
  NODE_ENV: "production"
  DB_USER: "inception-studio-db"
  DB_PASSWORD: "$Dev0055##"
  DB_NAME: "inception-studio-production"

  #BKASH
  BKASH_NUMBER: "01868984364"

  # Mail
  MAIL_HOST: "smtp.gmail.com"
  MAIL_PORT: "465"
  MAIL_SECURE: "true"
  MAIL_USER: "no-reply@inception.games"
  MAIL_PASS: "ogbb juwo qrhi pipc"
  MAIL_FROM: "no-reply@inception.games"

beta_settings:
  cloud_sql_instances: inception-games:asia-northeast1:inception-studio-db

handlers:
  - url: /.*
    script: auto
